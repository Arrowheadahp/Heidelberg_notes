The universal template for change is $$ new = old + \alpha(target-old) $$The alpha changes from approach to approach. 

When $\alpha == 1/n$ then it is called the sample average.
When $\alpha$ is constant, then the newer rewardsare given more importance and the older rewards are $*\alpha^t$ so vanishes eventually.