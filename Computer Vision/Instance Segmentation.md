This differentiates between instances of the same class ,Car A and Car B for example.
1. Faster R-CNN localizes and recognizes the objects
2. A semantic segmentation network is run for each object.
Example: Mask R-CNN