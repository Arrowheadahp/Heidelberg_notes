It is like a single neuron in a brain.

It takes in n inputs X
It has n weights W
bias b
[[Activation Functions]] f
output  $$y =f(w^T x +b)$$
The problem with [[Perceptron]] is that it cannot solve the XOR problem. So layering perceptrons one after the other creates the [[Neural Network]] and the [[Deep Neural Networks]].