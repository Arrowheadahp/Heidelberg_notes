### Support Vector Machines
Linear Classifier that maximises the separation between 2 classes. It can also use kernel functions to change the basis and solve the maximum magin for overlapping classifications as well.
