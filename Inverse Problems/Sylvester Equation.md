Minimising the 3 errors sum $\Gamma=\alpha_1spread(N)+\alpha_2spread(R) +\alpha_3size(cov(f))$ , we get $$\frac{\partial\Gamma}{\partial A^{-I}}\gets0 $$$$\alpha_1(A^TA)A^{-1}+A^{-1}(\alpha_2(AA^T)+\alpha_3(cov(g))) =(\alpha_1+\alpha_2)A^T $$Here the alphas are weighted accordingly
1. Data resolution: The reconstructed model tries to reproduce the data perfectly
2. Model Resolution: If true model has sharp feature, the resolution should also have it
3. Distance weighted spread: ghosting of localized features