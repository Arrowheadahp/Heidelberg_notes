Finding the best [[Policy]] $\pi$ that maximises the [[State Value]]. $$\pi^* \in arg\ max_\pi v_\pi(s) \ \forall s$$
$\pi^*$ is optimum when it is at least as good as every other policy at every state.

This is found by [[Policy Iteration]].