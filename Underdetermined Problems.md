These are the inverse problems that don't have enough data to get estimated parameters.
This happens when $(A^TA)^{-1}$ does not exist.
This happens when m>n. 
The solution is to get the solution with the minimum length.
#tobeunderstood 
The final equations come to 
1. $g=Af$
2. $2f-A^T\lambda=0$
Solving these 2 equations comes to 
$$f=A^T(AA^T)^{-1}g$$