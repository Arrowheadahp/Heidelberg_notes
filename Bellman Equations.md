To calculate the [[State Value Function]], We need the [[Return]], which depends on the entire future trajectory. Bellman's Idea is to replace the huge path expectation by a recursive one-step decomposition.

